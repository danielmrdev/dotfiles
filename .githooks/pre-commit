#!/bin/sh
set -eu

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks is required to commit dotfiles. Install it first." >&2
  exit 1
fi

exec gitleaks protect --staged --redact --no-banner
