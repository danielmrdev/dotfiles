[Unit]
Description=Time Machine backup to hdd-local

[Timer]
Unit=omarchy-time-machine@hdd-local.service
OnCalendar=*-*-* 14:30:00
# If the machine was off at that moment, run as soon as it comes back.
Persistent=true
RandomizedDelaySec=15m

[Install]
WantedBy=timers.target
