# Extra autostart processes
# exec-once = uwsm-app -- my-service

# SUPER+TAB app switcher (macOS-style) - handled by hyprshell daemon

# hy3: auto-tab workspace 4 when on laptop screen only
exec-once = ~/.config/hypr/hy3-layout-watch.sh
exec-once = focusd --daemon

